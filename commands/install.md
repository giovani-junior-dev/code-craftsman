---
description: Install code-craftsman tooling in current project
---

Run `bash ~/.claude/skills/code-craftsman/install.sh` in current directory.

Installs: biome, knip, stryker, dependency-cruiser, jest config, hooks, baseline.
